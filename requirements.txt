fastapi
uvicorn[standard]
requests
beautifulsoup4

# Импортируются напрямую (pydantic — модели ответов, urllib3 — политика
# повторов у HTTP-клиента Elasticsearch). Приходят и транзитивно, с
# fastapi и requests, но полагаться на это нельзя: транзитивная
# зависимость может исчезнуть в любом их выпуске.
pydantic>=2,<3
urllib3>=1.26,<3

lxml
pymupdf
openpyxl
python-docx
elasticsearch>=8.13,<9
python-dotenv
tenacity

# 2.5.0: подготовка изображения перед OCR.
# Pillow уже приходил транзитивно, теперь зависимость явная.
numpy>=1.20,<3
Pillow>=9,<12

# 2.5.0: форма входа в раздел управления.
#
# Без этого пакета FastAPI отказывается регистрировать обработчик с
# Form(...) — причём при импорте модуля, а не при первом запросе.
# То есть API не запустится вообще, а не «форма не будет работать».
python-multipart>=0.0.6
